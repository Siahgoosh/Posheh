# Desktop build shares the Flutter mobile codebase.
# Build for Windows:
#   cd mobile && flutter build windows --release
#
# Build for macOS:
#   cd mobile && flutter build macos --release
#
# Build for Linux:
#   cd mobile && flutter build linux --release
