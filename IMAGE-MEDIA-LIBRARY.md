# IMAGE-MEDIA-LIBRARY

Existing `/admin/blog/media` remains for uploads.

AI-generated covers stored on public disk with SEO filenames; previous cover URL not deleted automatically on approve (replace only post field). Usage checks recommended before manual delete.
