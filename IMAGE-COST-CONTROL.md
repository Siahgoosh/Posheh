# IMAGE-COST-CONTROL

Scopes in `blog_image_cost_limits`: per_image, per_article, daily, monthly.

Inactive by default. Paid generation blocked when `require_budget` and no active limits while estimated cost > 0.

Mock provider cost = 0.
