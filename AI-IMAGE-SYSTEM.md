# AI-IMAGE-SYSTEM

Provider-agnostic blog image generation for Posheh.

- Interface: `ImageProviderInterface`  
- Providers: `mock` (default), `openai` (opt-in)  
- Audit → Brief → Queue → Generate → Preview → Human Approve → Publish cover  
- Illustrative by default; never presented as real property photo  
