# IMAGE-AUDIT-REPORT

Run:

```bash
php artisan blog:image-audit --bootstrap --dry-run --limit=400
```

Outputs scanned / needs / skipped and dry-run plan. Live totals depend on production article corpus after migrate.
